# Tasks

- expand the context window and request limit for the app directory generation and database generation
- When the app builds the ts template files are causing build errors. Starter kit plan needs to have a step to tell the compiler to ignore the template files
- shadow not applied in component creation
- split the process into multiple plans/phases
