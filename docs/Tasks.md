# Tasks

- Add starter kits files
  - template files
  - documentation for tailwind, schadcn, supabase, protected routes,
- Write prompt with starter kit instructions
- Build beep.buzz and refine starter kit
- expand the context window and request limit for the app directory generation and database generation
- Add a arch theme
- update the plan to specify series processing (not parallel)
- Update the plan to specify that the user needs to login with "supabase login"
- prevent any "filling in the gaps" if it can't do something. Get it to stop and ask for clarity
- ensure that we link "supabase link"
