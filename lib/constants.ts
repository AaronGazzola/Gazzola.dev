export enum Role {
  User = "user",
  AI = "ai",
  Admin = "admin",
}

export enum Method {
  Post = "POST",
  Get = "GET",
}

export enum Question {
  About = "about",
  Porfiolio = "portfolio",
  Reviews = "reviews",
  Availability = "availability",
  Contact = "contact",
}
